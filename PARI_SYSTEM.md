# PARI Tennis System (рабочий документ, 2026-09-14, вечер)

## Что это
Автономный сбор live-данных тенниса с pari.ru + поиск валуйных паттернов.
Владелец ставит сам (Марафон/Винлайн/Лига), система даёт сигналы, не ставит.

## Архитектура
- **APK `com.pari.collector`** (`/public/pari-apk`, сборка `build.sh`): foreground-сервис,
  опрос API линии каждые 10 сек (listBase один раз + дельты по version-курсору),
  пишет `line-YYYY-ММ-ДД-HH.jsonl` в `/sdcard/Android/data/com.pari.collector/files/`.
  Кэфы — дифами (только изменения + часовой кадр), прематч — по изменению win-пары,
  финалы/фреши/блокировки/счётчики трафика внутри. Установка через adb в /data/local/tmp.
- **Протокол** (вскрыт из JS-бандлов): `line-{lb51,lb01,vk01}.pb06e2-resources.com`,
  `scopeMarket=2300`, `sysId=22`. 921=П1, 923=П2. Каталог `/line/factorsCatalog/tables`.
- **Python** (`/public`): `pari_line.py` (тот же протокол, бэкап/тесты),
  `pari_view.py` (отчёты + ветер), `pari_patterns.py` (геймы/брейки/триггеры 2/3/7, сеты),
  `pari_backtest.py` (триггеры A/B/C/D), `pari_paper.py` (бумага: add/settle/auto/void/report),
  `pari_profiles.py` (истории TE, кэш `_urlcache.json`), `pari_lib.py` (контуры/страны),
  `pari_obzor.py` (браузерный глубокий разбор одного матча — только разово!).
- **Данные**: `/public/pari_backup/` (пул с телефона, `pari_backup.sh`),
  `/public/night/`, `/public/profiles/`, `/public/paper.jsonl`.

## Правила денег (железные)
- Горизонты только game/set/match/prematch. Секундные движения запрещены.
- Контуры: LAB 0.5u (ITF/UTR/квалы), MAIN 1u, ASYM 1.5u. Порог edge 12%.
- Каждая ставка: точка выхода ДО входа (`--plan`). 3 проигрыша в день = стоп.
- Бумага первее денег. Сверка только по реальным кэфам из снимков.

## Проверено данными (накопительно; вечер 14.09: ~500 геймов, 60+ сетов)
- Брейки ~26%; обратные: Сан-Паулу 35%, UTR-w-Aus 38%, остальные 15-25%.
  Тактика только турнир-специфична.
- T7 (сухой старт → низ) отвергнута: ~50% vs база. A (0-40 на фаворите): 6/8 +1.16u.
- ASYM-TB первый хит: андердог 8.0 взял тай, +10.5u. M (марковская цена матча):
  3W-5L +0.03u на первых 8 — калибровка продолжается.
- Победитель 1-го сета берёт 2-й в ~67%. Зеркала (6-3→1-6) реальны, редки.

## Марковская цена (pari_lib.py)
- `game_fair(p, score)` — честная P(гейм) из очков; `match_fair(...)` — P(матч).
- `serve_point_prob` — p из live-статы (эйсы/двойные) иначе база тура (М 0.63/Ж 0.585).
- Вьювер подсвечивает edge>12% («МОДЕЛЬ/МАТЧ ...!»), бумага пишет триггер M автоматом.
- Порог 12%: без биржи мелочь съедает дисперсия.

## Ритуалы
- Утро: `pari_backup.sh` → backtest → `paper auto` → `paper void` → `paper report`.
- Новый игрок без профиля: резолв URL TE (вебпоиск) → `_urlcache.json` (вечно).
- APK не трогать без нужды. Мой loop дублирует — держать выключенным.
- Коммит+пуш после каждого рабочего батча (данные владельца: Oroani).
